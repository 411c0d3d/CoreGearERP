Get-ChildItem -Recurse -Force | Where-Object { $_.FullName -notmatch '\\\.git\\' -and $_.FullName -notmatch '\\\.idea\\' -and $_.FullName -notmatch '\\bin\\' -and $_.FullName -notmatch '\\obj\\' -and $_.FullName -notmatch '\\node_modules\\' -and $_.FullName -notmatch '\\\.(angular)\\' -and $_.FullName -notmatch '\\Migrations\\' } | ForEach-Object { $depth = ($_.FullName.Split('\').Count - $PWD.Path.Split('\').Count - 1); $indent = '  ' * $depth; if ($_.PSIsContainer) { "$indent$($_.Name)/" } else { "$indent$($_.Name)" } }